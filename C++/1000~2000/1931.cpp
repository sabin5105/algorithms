#include<iostream>
#include<string>
#include<vector>
#include<algorithm>
#define MAX 100000
using namespace std;

int main()
{
	int n;
	cin >> n;

	vector<int> a;
	vector<int> b;

	for (int i = 0; i < n; i++)
	{
		int temp, temp1;
		cin >> temp >> temp1;
		a.push_back(temp);
		b.push_back(temp1);
	}

	
	
}